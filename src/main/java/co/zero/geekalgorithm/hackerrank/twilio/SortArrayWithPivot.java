package co.zero.geekalgorithm.hackerrank.twilio;

public final class SortArrayWithPivot {
    public static  int[] sort(int[] array) {
        if (array.length == 1) {
            return array;
        } else {
            int pivot = array.length / 2 - 1;
//            sort(array, pivot, 0, pivot);
        }

        return null;
    }

//    private static void sort(int[] array, int pivot, int start, int end) {
//
//    }
}